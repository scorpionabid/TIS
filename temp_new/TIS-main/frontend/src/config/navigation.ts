/**
 * Navigation config entry point.
 *
 * The implementation lives in `./navigation/` (split by domain).
 * This file exists so that `@/config/navigation` resolves correctly —
 * TypeScript prefers a .ts file over an identically-named directory.
 */
export * from './navigation/index';
