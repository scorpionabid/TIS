# ATİS Frontend Environment Configuration

# API Configuration
VITE_API_URL=http://localhost:8000/api
VITE_APP_URL=http://localhost:3000

# Authentication Method Toggle
# Set to 'false' to use session-based auth (requires backend AUTH_GUARD=web)
# Set to 'true' to use Bearer token auth (requires backend AUTH_GUARD=sanctum)
# Production recommendation: true (token-based API authentication)
VITE_ENABLE_BEARER_AUTH=true

# Feature Flags for GenericManagerV2 System
# Set to 'true' to enable new features

# Component-specific flags (direct override)
VITE_USE_NEW_TEACHER_MANAGER=false
VITE_USE_NEW_STUDENT_MANAGER=false  
VITE_USE_NEW_CLASS_MANAGER=false

# Feature-specific flags
VITE_ENABLE_BULK_ACTIONS=false
VITE_ENABLE_ADVANCED_FILTERS=false

# Master flag for A/B testing GenericManagerV2
VITE_ENABLE_GENERIC_MANAGER_V2=false

# Development Features
VITE_ENABLE_DEBUG_PANEL=true
VITE_ENABLE_PERFORMANCE_MONITORING=false

# Production overrides (automatically handled)
# VITE_ENABLE_DEBUG_PANEL=false (in production)
# VITE_ENABLE_PERFORMANCE_MONITORING=true (in production)